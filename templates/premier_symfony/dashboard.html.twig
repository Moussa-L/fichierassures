<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <!-- Lien vers le CSS de Bootstrap pour les styles -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <link rel="stylesheet" href="/CSS/dashboard.css">
    <!-- script de Bootstrap pour les fonctionnalités JavaScript -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" crossorigin="anonymous"></script>
    <!-- Lien pour les icônes Bootstrap -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
</head>

<body> 

    <nav class="navbar navbar-light bg-white" >
        <a class="navbar-brand" href="#">
            
            <img src="/images/logo.jpg" alt="Logo" width="75" height="75" class="d-inline-block align-top logo-dashboard">
        </a>
        <a href="#">Accueil</a>
        <a href="#">Calcul Clé NIR</a>
        <a href="#" ><i class="bi bi-person-fill icone-profil"></i> {{ user.nomPrenom }}</a>
        <i class="bi bi-power text-dark icone-power" id="bouttonDeco"></i>
    </nav>

    <!--Titre de la page-->
        <h1 class="titre-dashboard">FICHIER DES ASSURES</h1>
        
    <!--Formulaire-->

    <form class="formulaire" method="GET" action="{{ path('dashBoard') }}">

    <div class="colonne marge-colonne">
        <input type="text" name="nir" placeholder="NIR :" value="{{ app.request.query.get('nir', '') }}">
        <input type="text" name="macben" placeholder="NUM COMPOSTAGE :" value="{{ app.request.query.get('macben', '') }}">
        <input type="text" name="dateTraitement" placeholder="DATE DE TRAITEMENT:" value="{{ app.request.query.get('dateTraitement', '') }}">
    </div>

    <div class="colonne">
        <input type="text" name="prenom" placeholder="PRÉNOM :" value="{{ app.request.query.get('prenom', '') }}">
        <input type="text" name="nom" placeholder="NOM :" value="{{ app.request.query.get('nom', '') }}">
    </div>

    <div class="colonne marge-colonne">
        <input type="text" name="dateNaissance" placeholder="DATE DE NAISSANCE :" value="{{ app.request.query.get('dateNaissance', '') }}">
        <input type="text" name="nirBnf" placeholder="NIR (BNF):" value="{{ app.request.query.get('nirBnf', '') }}">
    </div>
    
    <button type="submit" class="btn btn-primary">Rechercher</button>
    <a href="{{ path('dashBoard') }}" class="btn btn-secondary">Réinitialiser</a>

    



    </form>


    
    <table id="resultats">

        <thead>
            <tr>
                <th>NIR</th>
                <th>NIR (BNF)</th>
                <th>Nom, Prénom</th>
                <th>Date naissance</th>
                <th>addresse Complete</th>
                <th>Date de traitement</th>
            </tr>
        </thead>


        <tbody id="corpsTableau">
        {% for a in Assures %}
        <tr>
            <td>{{ a.nir }}</td>
            <td>{{ a.nirBnf }}</td>
            <td>{{ a.nom ~ ' ' ~ a.prenom }}</td>
            <td>{{ a.dateNaissance ?: '' }}</td>
            <td>{{ a.adresseComplete }}</td>
            <td>{{ a.dateTraitement ?: '' }}</td>
        </tr>
        {% else %}
        <tr><td colspan="6">Aucun résultat</td></tr>
        {% endfor %}
        </tbody>


    </table>

    

</body>

</html>
